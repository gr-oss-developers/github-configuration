name: "Drift Check"

on:
#  schedule:
#    - cron: '*/15 * * * *'
 workflow_dispatch:

jobs:
  drift-check:
    secrets:
      tfc_token: ${{ secrets.TFC_TOKEN }}
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}
    uses: G-Research/github-terraformer/.github/workflows/drift-check.yaml@test/branches-default-hardcode
    with:
      gcss_ref: test/branches-default-hardcode
      tfc_org: "GR-OSS"
