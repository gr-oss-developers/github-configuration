name: "Drift Check"

on:
#  schedule:
#    - cron: '*/15 * * * *'
 workflow_dispatch:

jobs:
  drift-check:
    secrets:
      tfc_token: ${{ secrets.TFC_TOKEN }}
    uses: G-Research/github-terraformer/.github/workflows/drift-check.yaml@speed-up-terraform-run-with-targeting-phase1
    with:
      gcss_ref: speed-up-terraform-run-with-targeting-phase1
      tfc_org: "GR-OSS"