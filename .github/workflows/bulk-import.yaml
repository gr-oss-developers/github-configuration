name: Bulk import repos pipeline

on:
  workflow_dispatch:

jobs:
  bulk-import-repos:
    uses: G-Research/github-terraformer/.github/workflows/bulk-import.yaml@disable-state-refresh-in-plan
    with:
      gcss_ref: disable-state-refresh-in-plan
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}