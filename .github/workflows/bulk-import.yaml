name: Bulk import repos pipeline

on:
  workflow_dispatch:

jobs:
  bulk-import-repos:
    uses: G-Research/github-terraformer/.github/workflows/bulk-import.yaml@v0.3.0-rc.2
    with:
      gcss_ref: v0.3.0-rc.2
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}