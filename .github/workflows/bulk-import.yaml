name: Bulk import repos pipeline

on:
  workflow_dispatch:

jobs:
  bulk-import-repos:
    uses: G-Research/github-terraformer/.github/workflows/bulk-import.yaml@speed-up-terraform-run-with-targeting-phase1
    with:
      gcss_ref: speed-up-terraform-run-with-targeting-phase1
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}