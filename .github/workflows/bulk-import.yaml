name: Bulk import repos pipeline

on:
  workflow_dispatch:

jobs:
  bulk-import-repos:
    uses: G-Research/github-terraformer/.github/workflows/bulk-import.yaml@apply-from-plan-file
    with:
      gcss_ref: apply-from-plan-file
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}