name: Import organisation

on:
  workflow_dispatch:

jobs:
  import-org:
    uses: G-Research/github-terraformer/.github/workflows/import-org.yaml@test/rc-cycle
    with:
      gcss_ref: test/rc-cycle
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}
