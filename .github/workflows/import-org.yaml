name: Import organisation

on:
  workflow_dispatch:

jobs:
  import-org:
    uses: G-Research/github-terraformer/.github/workflows/import-org.yaml@test/branches-default-hardcode
    with:
      gcss_ref: test/branches-default-hardcode
    secrets:
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}
